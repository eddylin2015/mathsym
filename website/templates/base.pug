doctype html
html
  head
    title act
    meta(charset='utf-8')
    meta(name="viewport", content="width=device-width, initial-scale=1")
    link(rel='stylesheet', href='/stylesheets/bootstrap.min.css')
    link(rel='stylesheet', href='/stylesheets/main.css')
    script(src='/javascripts/jquery.js')    
    script(src='/javascripts/bootstrap.js')        
    style.
      a {color:white;}
  body
    nav.navbar.navbar-inverse
      .container-fluid
        .navbar-header
          button.navbar-toggle(type='button', data-toggle='collapse', data-target='#myNavbar')
            span.icon-bar
            span.icon-bar
            span.icon-bar
          a.navbar-brand(href="#") 點名登記
        .collapse.navbar-collapse#myNavbar
          ul.nav.navbar-nav
            li.active
              a(href="/internal/") 
                span(class="glyphicon") &#xe021
            li
              a(href="/internal/attrollcall") 班別
            li
              a(href="/internal/attrollcall?sect=Dept") 部門
            if att_pass  
              li
                a(href="/internal/attrollcall/myrollcall") 職員登記
            if act_c_id
              li
                a(href=`/internal/attrollcall/al_list/${act_c_id}?fn=${fn}`) 項目(#{act_c_id})            
            li
              a(href="/internal/attrollcall/grp_cnt_list") 班別報數
            li
              a(href="/internal/attrollcall/rollcall_list") 點到列表
            li
              a(href="/internal/attrollcall/leave_list") 請假列表
            li
              a(href="/internal/attrollcall/miss_list") 未跟踪列表
        ul.navbar-text.navbar-right
          if att_pass
            a(href="/internal/attrollcall/al_logout",style="color:green") (exit)#{att_pass.stud_ref}            
          if profile
            if profile.image
              img.img-circle(src=profile.image, width=24)
            span #{profile.displayName} &nbsp;
              //a(href='/logout') (logout)
          else
            //a(href='/login') Login
    .container
      block content
